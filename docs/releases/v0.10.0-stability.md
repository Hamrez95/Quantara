# Quantara v0.10.0 stability report

Verdict: **NOT STABLE**

Date: 2026-07-28  
Branch: `feat/47-signal-inbox-ux`  
Pull request: #48

## Completed checks

- بازبینی ویدیوی 80.8 ثانیه‌ای کاربر و تطبیق هشت مسئله با جریان محصول
- بازبینی تغییرات شاخه نسبت به `dev`
- کنترل delimiter/string/comment روی 15 فایل Dart تغییرکرده: بدون عدم‌تعادل
- کنترل دستی مرزهای ایمنی: paper-only و R:R >= 1.6
- اصلاح مدل ناقص `OwnerAlphaSettings`
- ایزوله کردن خطاهای WorkManager از مسیر تحلیل زنده

## Blocked checks

- `flutter analyze`
- `flutter test`
- Android release APK/AAB build
- smoke test روی دستگاه واقعی برای background notification
- regression و UI smoke test

### Exact blocker

محیط محلی Flutter/Dart SDK ندارد. سه workflow مرتبط GitHub Actions نیز پیش از اجرای هر step متوقف می‌شوند؛ jobها `steps: []` دارند و log قابل دریافت تولید نشده است. بنابراین علت رانر از شواهد موجود قابل تشخیص نیست.

آخرین runهای ثبت‌شده روی head Candidate نیز همین الگو را دارند:

- CI: `30346795949` — دو job ناموفق، هر دو بدون step/log
- Flutter client CI: `30346795945` — job `90234801580` بدون step/log
- Deploy stable PWA: `30346795996` — validation بدون step/log و deploy skipped

## Merge decision

- PR باید Draft بماند.
- ادغام در `dev` یا `main` مجاز نیست تا همه gateها سبز شوند.
- هیچ APK/AAB به‌عنوان خروجی پایدار منتشر نشده است.

## Required path to stable

1. بازیابی GitHub Actions runner یا اجرای `scripts/build-app.ps1 -Target android-apk` در محیط دارای Flutter.
2. رفع تمام خطاهای analyze/test/build.
3. نصب روی Android و بررسی نوتیفیکیشن پس‌زمینه پس از بستن اپ.
4. اجرای smoke test صندوق پیشنهادها، یادداشت، انقضا و تغییر استراتژی.
5. ثبت SHA256 artifact و سپس برداشتن Draft.

## Rollback

شاخه مستقل است و هیچ merge یا release انجام نشده؛ rollback فعلی با بستن PR #48 انجام می‌شود و `dev`/ `main` بدون تغییر می‌مانند.
