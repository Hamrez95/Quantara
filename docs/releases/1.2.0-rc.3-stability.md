# Quantara 1.2.0-rc.3 software stability report

Software candidate SHA: `bdf4c2c27159530d0830c23f276de43444e01d68`  
Version/build: `1.2.0-rc.3+127`  
Evidence date: 2026-09-26

## Automated evidence

| Gate | Evidence | Result |
| --- | --- | --- |
| Repository integrity | Actions run `36252014760`; .NET job `108431684947`; repository validation `108431685038` | PASS |
| Flutter format/analyze/tests/build + hot-path gate | run `36252014763`, job `108431685317`; exact candidate `bdf4c2c27159530d0830c23f276de43444e01d68` | PASS |
| Android API 34 cold start + in-place upgrade | job `108433475673`, diagnostics artifact `10909537145` | PASS |
| Android API 35 cold start + in-place upgrade | job `108433475703`, diagnostics artifact `10909712752` | PASS |
| Android API 36 cold start + in-place upgrade | job `108433475794`, diagnostics artifact `10909553156` | PASS |
| Windows desktop/service lifecycle + one-click installer | run `36252014768`, job `108431685222`, artifact `10909377275`; exact candidate `bdf4c2c27159530d0830c23f276de43444e01d68`; packaged installer install/uninstall smoke included | PASS |
| Android installable QA artifact | artifact `10909022646`, exercised by the API 34/35/36 matrix | PASS |
| PWA QA artifact | artifact `10909501964` from the exact candidate | PASS |

The final software candidate is `bdf4c2c27159530d0830c23f276de43444e01d68`. Repository, Flutter/Android and Windows evidence above was produced directly from that exact candidate. The manual-trade Setup Inbox fix changes only UI admission to the existing forced preflight: authoritative Bitunix account truth is still refreshed before the sheet opens and checked again immediately before any exchange mutation.

The Windows workflow exercises the fail-closed service lifecycle and the packaged installer path: service install in stopped/disarmed state, authenticated read-only status verification, native app/Inno packaging, packaged installer installation with the service still stopped/disarmed, and packaged uninstall smoke. The Android matrix installs and cold-starts the candidate application tree and performs an in-place upgrade from the previous-base QA APK on APIs 34, 35 and 36.

## Stability verdict

**Software stability evidence: PASS.**

This verdict is intentionally limited to automated software/release-engineering evidence. It does not prove exchange-side correctness, profitability, physical-device battery/network behavior, permanent-signing continuity, or store/production readiness.

The #543 performance pass preserves fail-closed behavior: missing/stale account evidence, risk/protection/reconciliation failures, interrupted state, and unsafe restart conditions cannot grant new-entry authority.

## Explicit pending gates

- Physical-device + permanent-signing validation: **PENDING**.
- Production/store publication: **EXPLICIT OWNER APPROVAL REQUIRED**.

The machine-readable evidence contract is `docs/releases/current-readiness.json`.
