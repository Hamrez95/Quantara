# Quantara 1.2.0-rc.3 software stability report

Software candidate SHA: `5d60dde9d9200fb4fd5dac96b70142e6e9f87404`  
Version/build: `1.2.0-rc.3+127`  
Evidence date: 2026-09-24

## Automated evidence

| Gate | Evidence | Result |
| --- | --- | --- |
| Repository integrity | Actions run `36064669037`; .NET job `107851519505`; repository validation `107851519165` | PASS |
| Flutter format/analyze/tests/build + hot-path gate | run `36016725624`, job `107691424490`; final app tree ancestor `19b7bd3856bb4cc66a8c405631010e3a1bf07e11` | PASS |
| Android API 34 cold start + in-place upgrade | job `107696417014`, diagnostics artifact `10815423169` | PASS |
| Android API 35 cold start + in-place upgrade | job `107696417088`, diagnostics artifact `10815423182` | PASS |
| Android API 36 cold start + in-place upgrade | job `107696416899`, diagnostics artifact `10815981430` | PASS |
| Windows desktop/service lifecycle + one-click installer | run `36064669109`, job `107851818697`, artifact `10836925107`; release-tree ancestor `3f99479a407369c1b27927851a5a8b4546f431af`; packaged installer install/uninstall smoke included | PASS |
| Android installable QA artifact | artifact `10815178621`, exercised by the API 34/35/36 matrix | PASS |
| PWA QA artifact | artifact `10815317531` from the final application tree | PASS |

The final software candidate is `5d60dde9d9200fb4fd5dac96b70142e6e9f87404`. The Flutter/Android and Windows evidence above comes from candidate ancestors whose relevant trees are unchanged in the final candidate; `.github/workflows/release-readiness.yml` independently verifies that condition with ancestor and path-diff checks before accepting the evidence.

The Windows workflow exercises the fail-closed service lifecycle and the packaged installer path: service install in stopped/disarmed state, authenticated read-only status verification, native app/Inno packaging, packaged installer installation with the service still stopped/disarmed, and packaged uninstall smoke. The Android matrix installs and cold-starts the candidate application tree and performs an in-place upgrade from the previous-base QA APK on APIs 34, 35 and 36.

## Stability verdict

**Software stability evidence: PASS.**

This verdict is intentionally limited to automated software/release-engineering evidence. It does not prove exchange-side correctness, profitability, physical-device battery/network behavior, permanent-signing continuity, or store/production readiness.

The #543 performance pass preserves fail-closed behavior: missing/stale account evidence, risk/protection/reconciliation failures, interrupted state, and unsafe restart conditions cannot grant new-entry authority.

## Explicit pending gates

- Physical-device + permanent-signing validation: **PENDING**.
- Production/store publication: **EXPLICIT OWNER APPROVAL REQUIRED**.

The machine-readable evidence contract is `docs/releases/current-readiness.json`.
