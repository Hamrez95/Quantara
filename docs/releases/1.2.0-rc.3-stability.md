# Quantara 1.2.0-rc.3 software stability report

Software candidate SHA: `e48162ff4c83bcd681bcd65afd594a3f44cb08dd`  
Version/build: `1.2.0-rc.3+127`  
Evidence date: 2026-09-06

## Automated evidence

| Gate | Evidence | Result |
| --- | --- | --- |
| Repository integrity | Actions run `34019258908`; .NET `101448678925`; repository validation `101448678807` | PASS |
| Flutter format/analyze/tests/build | run `34019258910`, job `101448689487` | PASS |
| Android API 34 cold start + in-place upgrade | job `101450040253`, diagnostics artifact `9985114572` | PASS |
| Android API 35 cold start + in-place upgrade | job `101450040141`, diagnostics artifact `9985114851` | PASS |
| Android API 36 cold start + in-place upgrade | job `101450040122`, diagnostics artifact `9985121423` | PASS |
| Windows desktop/service lifecycle | run `34019258892`, check `101448691653`, artifact `9985117211` | PASS |
| Android installable QA artifact | artifact `9985076805`, exercised by the API 34/35/36 matrix | PASS |

The Windows workflow exercises the fail-closed service lifecycle: install in stopped/disarmed state, start, authenticated read-only status verification, stop and uninstall. The Android matrix installs and cold-starts the candidate and performs an in-place upgrade from the previous-base QA APK on APIs 34, 35 and 36.

## Stability verdict

**Software stability evidence: PASS.**

This verdict is intentionally limited to automated software/release-engineering evidence. It does not prove exchange-side correctness, profitability, physical-device battery/network behavior, permanent-signing continuity, or store/production readiness.

The Global Pause restart regressions continue to require fail-closed behavior: corrupt/unreadable persisted pause state cannot grant scanning or entry authority, and restart/reconnect cannot auto-resume trading.

## Explicit pending gates

- Physical-device + permanent-signing validation: **PENDING**.
- Production/store publication: **EXPLICIT OWNER APPROVAL REQUIRED**.

The machine-readable evidence contract is `docs/releases/current-readiness.json`.
