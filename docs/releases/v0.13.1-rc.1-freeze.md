# Quantara 0.13.1-rc.1 tree freeze

Candidate source is frozen after this commit. Only CI evidence, artifact metadata and physical acceptance results may update the release tracker; code changes require a new RC build number.
