name: Temporary PR153 exact controller format

on:
  push:
    branches:
      - feat/152-professional-strategy-pack

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/152-professional-strategy-pack
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
          channel: stable
          cache: true
      - name: Format controller fixture
        working-directory: src/client/quantara_app
        run: dart format test/owner_alpha_controller_test.dart
      - name: Commit exact format
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/client/quantara_app/test/owner_alpha_controller_test.dart
          git commit -m "style: apply exact controller fixture format"
          git push origin HEAD:feat/152-professional-strategy-pack
