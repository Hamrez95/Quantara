name: Release regression fix

on:
  push:
    branches:
      - release/0.11.0-strategy-engine

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          ref: release/0.11.0-strategy-engine

      - name: Set up Dart
        uses: dart-lang/setup-dart@v1
        with:
          sdk: 3.12.2

      - name: Fix strategy fixture and responsive setup card
        working-directory: src/client/quantara_app
        run: |
          python - <<'PY'
          from pathlib import Path

          strategy = Path('lib/features/owner_alpha/data/advanced_strategy_engine.dart')
          text = strategy.read_text()
          text = text.replace('indicators.atr14 * 1.25,', 'indicators.atr14 * 2.25,')
          strategy.write_text(text)

          dashboard = Path('lib/features/owner_alpha/presentation/owner_alpha_dashboard.dart')
          text = dashboard.read_text()
          old = '''          Row(
            children: [
              Expanded(
                child: CheckboxListTile(
                  contentPadding: EdgeInsets.zero,
                  dense: true,
                  controlAffinity: ListTileControlAffinity.leading,
                  value: taken,
                  onChanged: (value) => onTakenChanged(value ?? false),
                  title: Text(strings.taken),
                  subtitle: Text(strings.markTaken),
                ),
              ),
              const SizedBox(width: 8),
              FilledButton.tonalIcon(
                onPressed: onTap,
                icon: const Icon(Icons.candlestick_chart_rounded),
                label: Text(strings.inspectChart),
              ),
            ],
          ),'''
          new = '''          LayoutBuilder(
            builder: (context, constraints) {
              final takenControl = InkWell(
                borderRadius: BorderRadius.circular(12),
                onTap: () => onTakenChanged(!taken),
                child: Padding(
                  padding: const EdgeInsets.symmetric(vertical: 6),
                  child: Row(
                    children: [
                      Checkbox(
                        value: taken,
                        onChanged: (value) => onTakenChanged(value ?? false),
                      ),
                      const SizedBox(width: 6),
                      Expanded(
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Text(strings.taken),
                            Text(
                              strings.markTaken,
                              style: Theme.of(context).textTheme.bodySmall,
                            ),
                          ],
                        ),
                      ),
                    ],
                  ),
                ),
              );
              final chartButton = FilledButton.tonalIcon(
                onPressed: onTap,
                icon: const Icon(Icons.candlestick_chart_rounded),
                label: Text(strings.inspectChart),
              );
              if (constraints.maxWidth < 520) {
                return Column(
                  crossAxisAlignment: CrossAxisAlignment.stretch,
                  children: [
                    takenControl,
                    const SizedBox(height: 8),
                    Align(
                      alignment: AlignmentDirectional.centerEnd,
                      child: chartButton,
                    ),
                  ],
                );
              }
              return Row(
                children: [
                  Expanded(child: takenControl),
                  const SizedBox(width: 8),
                  chartButton,
                ],
              );
            },
          ),'''
          if old not in text:
              raise SystemExit('setup-card block not found')
          dashboard.write_text(text.replace(old, new))
          PY
          dart format \
            lib/features/owner_alpha/data/advanced_strategy_engine.dart \
            lib/features/owner_alpha/presentation/owner_alpha_dashboard.dart

      - name: Commit fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/client/quantara_app/lib/features/owner_alpha
          if ! git diff --cached --quiet; then
            git commit -m "Fix adaptive reversal coverage and compact setup layout"
            git push origin HEAD:release/0.11.0-strategy-engine
          fi
