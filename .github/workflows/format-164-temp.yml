name: Temporary PR164 Dart formatter

on:
  push:
    branches:
      - feat/163-portfolio-budgeted-local-live
    paths:
      - '.github/workflows/format-164-temp.yml'

permissions:
  contents: write

concurrency:
  group: pr164-final-format
  cancel-in-progress: true

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: stable
          cache: true
      - name: Format signing contract test
        shell: bash
        run: |
          set -euo pipefail
          cd src/client/quantara_app
          flutter pub get
          dart format test/android_preview_signing_source_test.dart
          cd ../../..
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/client/quantara_app/test/android_preview_signing_source_test.dart
          if git diff --cached --quiet; then
            echo 'Already formatted.'
            exit 0
          fi
          git commit -m 'style: format signing release contract test'
          git push origin HEAD:feat/163-portfolio-budgeted-local-live
