name: Temporary issue 165 historical journal backfill

on:
  push:
    branches:
      - feat/163-portfolio-budgeted-local-live

permissions:
  contents: write

concurrency:
  group: feat-165-historical-journal-backfill
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/163-portfolio-budgeted-local-live
          fetch-depth: 0
      - name: Apply verified historical close backfill
        run: python3 .github/scripts/feat165_historical_journal_backfill.py
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: stable
          cache: true
      - name: Restore packages
        working-directory: src/client/quantara_app
        run: flutter pub get
      - name: Format patch
        working-directory: src/client/quantara_app
        run: dart format lib test
      - name: Analyze patch
        working-directory: src/client/quantara_app
        run: flutter analyze
      - name: Run historical close and safety regressions
        working-directory: src/client/quantara_app
        run: >-
          flutter test
          test/trading_journal_exchange_backfill_test.dart
          test/bitunix_pnl_mapper_physical_canary_test.dart
          test/trading_journal_foreground_merge_test.dart
          test/database_trading_journal_store_test.dart
          test/physical_canary_regression_source_test.dart
      - name: Commit patch
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No patch changes remain.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/client/quantara_app
          git commit -m 'fix: backfill verified historical exchange closures into journal (#165)'
          git push origin HEAD:feat/163-portfolio-budgeted-local-live
