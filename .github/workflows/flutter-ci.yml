name: Temporary apply issue 158 runtime resilience

on:
  push:
    branches:
      - fix/158-local-live-runtime-resilience

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/158-local-live-runtime-resilience
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
          channel: stable
          cache: true

      - name: Apply resilience patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          root = Path('src/client/quantara_app')

          def replace_once(path: Path, old: str, new: str, label: str) -> None:
              text = path.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected one match, found {count}')
              path.write_text(text.replace(old, new))

          service = root / 'lib/features/auto_trade/application/local_live_trade_service.dart'
          replace_once(
              service,
              "import '../domain/local_live_trade_models.dart';\nimport '../domain/profit_lock_stop_policy.dart';",
              "import '../domain/local_live_cycle_readiness.dart';\nimport '../domain/local_live_trade_models.dart';\nimport '../domain/profit_lock_stop_policy.dart';",
              'service import',
          )
          replace_once(
              service,
              """      if (!account.authoritativePnl.isVerified ||
          !account.authoritativePnl.fillsAvailable) {
        _entriesEnabled = false;
        _auditEvent(
          'pnl_projection_block',
          account.authoritativePnl.warning ??
              'Exchange PnL history is unavailable or unverified.',
        );
      }
      _sessionStartEquity ??= account.estimatedEquity;""",
              """      final managedPositionIds = _managed
          .map((position) => position.positionId)
          .where((positionId) => positionId.isNotEmpty)
          .toSet();
      final hasUnmanagedExchangeExposure = positions.any(
        (position) =>
            position.quantity > 0 &&
            !managedPositionIds.contains(position.positionId),
      );
      final readiness = LocalLiveCycleReadinessPolicy.evaluate(
        hasManagedExposure: _managed.isNotEmpty,
        hasUnmanagedExchangeExposure: hasUnmanagedExchangeExposure,
        pnlVerified: account.authoritativePnl.isVerified,
        fillsAvailable: account.authoritativePnl.fillsAvailable,
      );
      String? cycleWarning;
      switch (readiness) {
        case LocalLiveCycleReadiness.ready:
          break;
        case LocalLiveCycleReadiness.emptyAccountHistoryPending:
          _auditEvent(
            'pnl_projection_pending_empty_account',
            account.authoritativePnl.warning ??
                'Historical fill data is pending for the empty account; entry readiness is unchanged.',
          );
          break;
        case LocalLiveCycleReadiness.managedExposureHistoryBlocked:
          _entriesEnabled = false;
          cycleWarning =
              'Managed exchange exposure requires verified fill history. New entries are blocked while existing protection is reconciled.';
          _auditEvent(
            'pnl_projection_block',
            account.authoritativePnl.warning ?? cycleWarning,
          );
          break;
        case LocalLiveCycleReadiness.unmanagedExposureBlocked:
          _entriesEnabled = false;
          cycleWarning =
              'An unmanaged exchange position was detected. New entries are blocked and Quantara did not adopt the position.';
          _auditEvent('unmanaged_exposure_block', cycleWarning);
          break;
      }
      _sessionStartEquity ??= account.estimatedEquity;""",
              'service readiness',
          )
          replace_once(
              service,
              """        profitLockWarning ??
            (_entriesEnabled
                ? 'Local live scan and exchange reconciliation completed.'
                : 'Only exchange-protected positions are being reconciled.'),""",
              """        cycleWarning ??
            profitLockWarning ??
            (_entriesEnabled
                ? 'Local live scan and exchange reconciliation completed.'
                : _managed.isEmpty
                ? 'New entries are paused; no Local Live position is open.'
                : 'Only exchange-protected positions are being reconciled.'),""",
              'service cycle status',
          )
          replace_once(
              service,
              """      notificationTitle: state == LocalLiveTradeState.circuitBreaker
          ? 'Quantara · Circuit breaker'
          : state == LocalLiveTradeState.managingOnly
          ? 'Quantara · Managing protected positions'
          : 'Quantara · Local live canary',
      notificationText: _notificationPnlText(),""",
              """      notificationTitle: _notificationTitle(state),
      notificationText: _notificationPnlText(state),""",
              'service notification call',
          )
          old_notification = """  String _notificationPnlText() {
    final projection = _sessionPnlProjection;
    if (projection == null) {
      return '${_managed.length} open · session PnL unavailable';
    }
    final net = projection.accountNetRealized;
    final unrealized = projection.accountUnrealized;
    final netText = net.isAvailable
        ? '${net.value! >= 0 ? '+' : ''}${net.value!.toStringAsFixed(2)} ${net.currency}'
        : 'unavailable';
    final openText = unrealized.isAvailable
        ? '${unrealized.value! >= 0 ? '+' : ''}${unrealized.value!.toStringAsFixed(2)} ${unrealized.currency}'
        : 'unavailable';
    return '${_managed.length} open · session net $netText · open $openText';
  }
"""
          new_notification = """  String _notificationTitle(LocalLiveTradeState state) {
    if (state == LocalLiveTradeState.circuitBreaker) {
      return _notificationCopy(
        'Quantara · توقف ایمنی',
        'Quantara · Circuit breaker',
      );
    }
    if (state == LocalLiveTradeState.starting) {
      return _notificationCopy(
        'Quantara · شروع ترید محلی',
        'Quantara · Starting local live',
      );
    }
    if (state == LocalLiveTradeState.managingOnly) {
      return _managed.isEmpty
          ? _notificationCopy(
              'Quantara · ورودهای جدید متوقف',
              'Quantara · Entries paused',
            )
          : _notificationCopy(
              'Quantara · مدیریت پوزیشن محافظت‌شده',
              'Quantara · Managing protected positions',
            );
    }
    return _notificationCopy(
      'Quantara · ترید واقعی محلی',
      'Quantara · Local live canary',
    );
  }

  String _notificationPnlText(LocalLiveTradeState state) {
    if (_managed.isEmpty) {
      if (state == LocalLiveTradeState.starting || _lastExchangeSync == null) {
        return _notificationCopy(
          'در حال همگام‌سازی حساب Bitunix',
          'Syncing Bitunix account',
        );
      }
      return _entriesEnabled
          ? _notificationCopy(
              'بدون پوزیشن باز · در حال اسکن نمادهای منتخب',
              '0 open · scanning selected symbols',
            )
          : _notificationCopy(
              'بدون پوزیشن باز · ورود جدید متوقف است',
              '0 open · new entries paused',
            );
    }

    final projection = _sessionPnlProjection;
    if (projection == null) {
      return _notificationCopy(
        '${_managed.length} پوزیشن باز · در حال همگام‌سازی سود و زیان',
        '${_managed.length} open · syncing exchange PnL',
      );
    }
    final net = projection.accountNetRealized;
    final unrealized = projection.accountUnrealized;
    final pending = _notificationCopy('در انتظار', 'pending');
    final netText = net.isAvailable
        ? '${net.value! >= 0 ? '+' : ''}${net.value!.toStringAsFixed(2)} ${net.currency}'
        : pending;
    final openText = unrealized.isAvailable
        ? '${unrealized.value! >= 0 ? '+' : ''}${unrealized.value!.toStringAsFixed(2)} ${unrealized.currency}'
        : pending;
    return _notificationCopy(
      '${_managed.length} باز · خالص جلسه $netText · باز $openText',
      '${_managed.length} open · session net $netText · open $openText',
    );
  }

  String _notificationCopy(String fa, String en) =>
      _configuration?.languageCode == 'en' ? en : fa;
"""
          replace_once(
              service,
              old_notification,
              new_notification,
              'service notification copy',
          )

          runtime = root / 'lib/features/owner_alpha/data/realtime_production_runtime.dart'
          replace_once(
              runtime,
              "  bool get healthy => health?.discoveryHealthy == true && error == null;",
              "  bool get operational => health?.operational == true && error == null;\n\n  bool get healthy => health?.discoveryHealthy == true && error == null;",
              'monitor operational getter',
          )
          runtime_text = runtime.read_text()
          host_start = runtime_text.index('final class RealtimeMarketHost')
          host_end = runtime_text.index('abstract final class RealtimeSettingsUniverse')
          new_host = r'''final class RealtimeMarketHost
    extends ValueNotifier<RealtimeMarketMonitorSnapshot>
    with WidgetsBindingObserver {
  RealtimeMarketHost({
    required this.runtime,
    this.onLanguageChanged,
    this.onDispose,
    this.pollInterval = const Duration(seconds: 1),
    this.backgroundPauseGrace = const Duration(seconds: 3),
    this.degradedRetryInterval = const Duration(minutes: 5),
  }) : super(const RealtimeMarketMonitorSnapshot.initial()) {
    if (pollInterval < const Duration(milliseconds: 250)) {
      throw ArgumentError.value(pollInterval, 'pollInterval');
    }
    if (backgroundPauseGrace < Duration.zero ||
        backgroundPauseGrace > const Duration(seconds: 30)) {
      throw ArgumentError.value(backgroundPauseGrace, 'backgroundPauseGrace');
    }
    if (degradedRetryInterval <= Duration.zero ||
        degradedRetryInterval > const Duration(hours: 1)) {
      throw ArgumentError.value(
        degradedRetryInterval,
        'degradedRetryInterval',
      );
    }
  }

  final RealtimeMarketRuntimeLifecycle runtime;
  final ValueChanged<String>? onLanguageChanged;
  final VoidCallback? onDispose;
  final Duration pollInterval;
  final Duration backgroundPauseGrace;
  final Duration degradedRetryInterval;
  Timer? _timer;
  Timer? _backgroundPauseTimer;
  Future<void> _operationTail = Future.value();
  DateTime? _degradedSinceUtc;
  bool _degradedRecoveryScheduled = false;
  bool _initialized = false;
  bool _disposed = false;

  Future<void> initialize() => _serialize(() async {
    if (_disposed || _initialized) return;
    _initialized = true;
    WidgetsBinding.instance.addObserver(this);
    try {
      await runtime.start();
      _publish();
      _timer = Timer.periodic(pollInterval, (_) => _publish());
    } on Object catch (error) {
      _publish(error: error.toString());
    }
  });

  void setLanguage(String languageCode) {
    if (languageCode != 'fa' && languageCode != 'en') return;
    onLanguageChanged?.call(languageCode);
  }

  @override
  void didChangeAppLifecycleState(AppLifecycleState state) {
    switch (state) {
      case AppLifecycleState.resumed:
        _backgroundPauseTimer?.cancel();
        _backgroundPauseTimer = null;
        unawaited(_serialize(_resume));
        return;
      case AppLifecycleState.inactive:
      case AppLifecycleState.hidden:
        return;
      case AppLifecycleState.paused:
        _scheduleBackgroundPause();
        return;
      case AppLifecycleState.detached:
        _backgroundPauseTimer?.cancel();
        _backgroundPauseTimer = null;
        unawaited(_serialize(_pause));
        return;
    }
  }

  void _scheduleBackgroundPause() {
    if (_disposed || !_initialized) return;
    _backgroundPauseTimer?.cancel();
    _backgroundPauseTimer = Timer(backgroundPauseGrace, () {
      _backgroundPauseTimer = null;
      unawaited(_serialize(_pause));
    });
  }

  Future<void> _resume() async {
    if (_disposed || !_initialized) return;
    try {
      if (runtime.state == RealtimeMarketRuntimeState.paused) {
        await runtime.resume();
      } else if (runtime.state == RealtimeMarketRuntimeState.idle) {
        await runtime.start();
      }
      _publish();
    } on Object catch (error) {
      _publish(error: error.toString());
    }
  }

  Future<void> _pause() async {
    if (_disposed || !_initialized) return;
    final state = runtime.state;
    if (state == RealtimeMarketRuntimeState.paused ||
        state == RealtimeMarketRuntimeState.idle ||
        state == RealtimeMarketRuntimeState.stopped) {
      return;
    }
    try {
      await runtime.pause();
      _degradedSinceUtc = null;
      _publish();
    } on Object catch (error) {
      _publish(error: error.toString());
    }
  }

  void _publish({String? error}) {
    if (_disposed) return;
    RealtimeMarketHealthSnapshot? health;
    try {
      health = runtime.health;
    } on Object {
      health = null;
    }
    value = RealtimeMarketMonitorSnapshot(
      health: health,
      error: error,
      foregroundOnly: true,
    );
    _maybeRecoverDegraded(health, error: error);
  }

  void _maybeRecoverDegraded(
    RealtimeMarketHealthSnapshot? health, {
    required String? error,
  }) {
    if (_disposed ||
        error != null ||
        health?.degraded != true ||
        runtime.state != RealtimeMarketRuntimeState.live) {
      _degradedSinceUtc = null;
      return;
    }
    final now = DateTime.now().toUtc();
    final degradedSince = _degradedSinceUtc;
    if (degradedSince == null) {
      _degradedSinceUtc = now;
      return;
    }
    if (_degradedRecoveryScheduled ||
        now.difference(degradedSince) < degradedRetryInterval) {
      return;
    }

    _degradedRecoveryScheduled = true;
    _degradedSinceUtc = now;
    unawaited(
      _serialize(() async {
        try {
          if (!_disposed &&
              runtime.state == RealtimeMarketRuntimeState.live &&
              runtime.health.degraded) {
            await runtime.pause();
            await runtime.resume();
          }
          _publish();
        } on Object catch (recoveryError) {
          _publish(error: recoveryError.toString());
        } finally {
          _degradedRecoveryScheduled = false;
        }
      }),
    );
  }

  Future<void> _serialize(Future<void> Function() operation) {
    final result = _operationTail.then((_) => operation());
    _operationTail = result.then<void>(
      (_) {},
      onError: (Object _, StackTrace _) {},
    );
    return result;
  }

  @override
  void dispose() {
    if (_disposed) return;
    _disposed = true;
    _timer?.cancel();
    _backgroundPauseTimer?.cancel();
    WidgetsBinding.instance.removeObserver(this);
    unawaited(runtime.stop().catchError((Object _) {}));
    onDispose?.call();
    super.dispose();
  }
}

'''
          runtime.write_text(
              runtime_text[:host_start] + new_host + runtime_text[host_end:]
          )
          replace_once(
              runtime,
              '      closedCandleLimit: 120,',
              '      closedCandleLimit: 64,',
              'production warm-up',
          )

          models = root / 'lib/features/owner_alpha/domain/realtime_market_runtime_models.dart'
          replace_once(
              models,
              """  bool get discoveryHealthy =>
      state == RealtimeMarketRuntimeState.live &&
      activeStreams > 0 &&
      quarantinedStreams == 0 &&
      activeShards > 0 &&
      liveShards == activeShards;

  bool get degraded =>
      state == RealtimeMarketRuntimeState.live && quarantinedStreams > 0;""",
              """  bool get operational =>
      state == RealtimeMarketRuntimeState.live &&
      activeStreams > 0 &&
      activeShards > 0 &&
      liveShards == activeShards;

  bool get discoveryHealthy => operational && quarantinedStreams == 0;

  bool get degraded => operational && quarantinedStreams > 0;""",
              'health semantics',
          )

          page = root / 'lib/features/owner_alpha/presentation/owner_alpha_page.dart'
          replace_once(
              page,
              '                  _LiveBoundaryStrip(realtimeMonitor: realtimeMonitor),',
              """                  if (destination != 5 &&
                      destination != 6 &&
                      destination != 7)
                    _LiveBoundaryStrip(realtimeMonitor: realtimeMonitor),""",
              'hide public strip from execution pages',
          )
          replace_once(
              page,
              """    final health = monitor.health;
    final healthy = monitor.healthy;
    final status = health == null""",
              """    final health = monitor.health;
    final operational = monitor.operational;
    final healthy = monitor.healthy;
    final degraded = health?.degraded == true;
    final statusColor = operational
        ? degraded
              ? QuantaraColors.warning
              : QuantaraColors.success
        : QuantaraColors.cyan;
    final status = health == null""",
              'live strip status color',
          )
          replace_once(
              page,
              """            RealtimeMarketRuntimeState.paused => strings.t(
              'پایش متوقف',
              'Monitoring paused',
            ),""",
              """            RealtimeMarketRuntimeState.paused => strings.t(
              'پایش پیشنهادها در پس‌زمینه متوقف است',
              'Signal monitoring is paused in the background',
            ),""",
              'paused wording',
          )
          replace_once(
              page,
              """          color: (healthy ? QuantaraColors.success : QuantaraColors.cyan)
              .withValues(alpha: 0.09),
          borderRadius: BorderRadius.circular(16),
          border: Border.all(
            color: (healthy ? QuantaraColors.success : QuantaraColors.cyan)
                .withValues(alpha: 0.25),
          ),""",
              """          color: statusColor.withValues(alpha: 0.09),
          borderRadius: BorderRadius.circular(16),
          border: Border.all(color: statusColor.withValues(alpha: 0.25)),""",
              'live strip decoration',
          )
          replace_once(
              page,
              """              Icon(
                healthy ? Icons.sensors_rounded : Icons.shield_outlined,
                color: healthy ? QuantaraColors.success : QuantaraColors.cyan,
              ),""",
              """              Icon(
                operational ? Icons.sensors_rounded : Icons.shield_outlined,
                color: statusColor,
              ),""",
              'live strip icon',
          )
          replace_once(
              page,
              """                        color: error != null || degradedDetail != null
                            ? scheme.error
                            : scheme.onSurfaceVariant,""",
              """                        color: error != null
                            ? scheme.error
                            : degradedDetail != null
                            ? QuantaraColors.warning
                            : scheme.onSurfaceVariant,""",
              'degraded warning color',
          )

          runtime_test = root / 'test/realtime_production_runtime_test.dart'
          test_text = runtime_test.read_text()
          pattern = re.compile(
              r"  test\('foreground host starts, pauses, resumes and stops safely', \(\) async \{.*?\n  \}\);\n",
              re.S,
          )
          replacement = r'''  test('transient inactive and hidden states do not pause monitoring', () async {
    final runtime = _FakeRuntime();
    final host = RealtimeMarketHost(
      runtime: runtime,
      pollInterval: const Duration(milliseconds: 250),
      backgroundPauseGrace: const Duration(milliseconds: 40),
    );

    await host.initialize();
    host.didChangeAppLifecycleState(AppLifecycleState.inactive);
    host.didChangeAppLifecycleState(AppLifecycleState.hidden);
    await Future<void>.delayed(const Duration(milliseconds: 60));

    expect(runtime.starts, 1);
    expect(runtime.pauses, 0);
    expect(runtime.state, RealtimeMarketRuntimeState.live);

    host.dispose();
    await Future<void>.delayed(Duration.zero);
    expect(runtime.stops, 1);
  });

  test('brief background transition is cancelled when the app resumes', () async {
    final runtime = _FakeRuntime();
    final host = RealtimeMarketHost(
      runtime: runtime,
      pollInterval: const Duration(milliseconds: 250),
      backgroundPauseGrace: const Duration(milliseconds: 60),
    );

    await host.initialize();
    host.didChangeAppLifecycleState(AppLifecycleState.paused);
    await Future<void>.delayed(const Duration(milliseconds: 15));
    host.didChangeAppLifecycleState(AppLifecycleState.resumed);
    await Future<void>.delayed(const Duration(milliseconds: 80));

    expect(runtime.pauses, 0);
    expect(runtime.resumes, 0);
    expect(runtime.state, RealtimeMarketRuntimeState.live);

    host.dispose();
  });

  test('sustained backgrounding pauses after grace and resumes safely', () async {
    final runtime = _FakeRuntime();
    final host = RealtimeMarketHost(
      runtime: runtime,
      pollInterval: const Duration(milliseconds: 250),
      backgroundPauseGrace: const Duration(milliseconds: 20),
    );

    await host.initialize();
    host.didChangeAppLifecycleState(AppLifecycleState.paused);
    await Future<void>.delayed(const Duration(milliseconds: 60));
    expect(runtime.pauses, 1);
    expect(runtime.state, RealtimeMarketRuntimeState.paused);

    host.didChangeAppLifecycleState(AppLifecycleState.resumed);
    await Future<void>.delayed(Duration.zero);
    await Future<void>.delayed(Duration.zero);
    expect(runtime.resumes, 1);
    expect(runtime.state, RealtimeMarketRuntimeState.live);

    host.dispose();
  });

  test('degraded monitoring receives one bounded recovery restart', () async {
    final runtime = _FakeRuntime(degraded: true);
    final host = RealtimeMarketHost(
      runtime: runtime,
      pollInterval: const Duration(milliseconds: 250),
      degradedRetryInterval: const Duration(milliseconds: 150),
    );

    await host.initialize();
    await Future<void>.delayed(const Duration(milliseconds: 340));

    expect(runtime.pauses, 1);
    expect(runtime.resumes, 1);
    expect(host.value.operational, isTrue);
    expect(host.value.healthy, isFalse);

    host.dispose();
  });
'''
          test_text, count = pattern.subn(replacement, test_text)
          if count != 1:
              raise SystemExit(
                  f'runtime lifecycle tests: expected one match, found {count}'
              )
          test_text = test_text.replace(
              'final class _FakeRuntime implements RealtimeMarketRuntimeLifecycle {\n  var starts = 0;',
              'final class _FakeRuntime implements RealtimeMarketRuntimeLifecycle {\n  _FakeRuntime({this.degraded = false});\n\n  final bool degraded;\n  var starts = 0;',
          )
          test_text = test_text.replace(
              '    configuredStreams: 8,\n    activeShards: 1,',
              '    configuredStreams: 8,\n    activeStreams: degraded ? 7 : 8,\n    quarantinedStreams: degraded ? 1 : 0,\n    activeShards: 1,',
          )
          runtime_test.write_text(test_text)

          application_test = root / 'test/realtime_market_application_test.dart'
          replace_once(
              application_test,
              """        expect(application.health.degraded, isTrue);
        expect(application.health.discoveryHealthy, isFalse);""",
              """        expect(application.health.degraded, isTrue);
        expect(application.health.operational, isTrue);
        expect(application.health.discoveryHealthy, isFalse);""",
              'degraded operational test',
          )

          source_test = root / 'test/local_live_runtime_resilience_source_test.dart'
          source_test.write_text(r'''import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Local Live uses exposure-aware PnL readiness without weakening safety', () {
    final source = File(
      'lib/features/auto_trade/application/local_live_trade_service.dart',
    ).readAsStringSync();

    expect(source, contains('LocalLiveCycleReadinessPolicy.evaluate'));
    expect(source, contains('emptyAccountHistoryPending'));
    expect(source, contains('unmanagedExposureBlocked'));
    expect(source, contains('placePositionStop'));
    expect(source, contains('placePartialTakeProfit'));
    expect(source, contains('closePositionReduceOnly'));
    expect(source, contains('for (var index = 0; index < 3; index++)'));
  });

  test('production realtime warm-up remains bounded and indicator-safe', () {
    final source = File(
      'lib/features/owner_alpha/data/realtime_production_runtime.dart',
    ).readAsStringSync();

    expect(source, contains('closedCandleLimit: 64'));
    expect(source, isNot(contains('closedCandleLimit: 120')));
    expect(source, contains('backgroundPauseGrace'));
    expect(source, contains('degradedRetryInterval'));
  });
}
''')
          PY

          dart format \
            src/client/quantara_app/lib/features/auto_trade/domain/local_live_cycle_readiness.dart \
            src/client/quantara_app/lib/features/auto_trade/application/local_live_trade_service.dart \
            src/client/quantara_app/lib/features/owner_alpha/data/realtime_production_runtime.dart \
            src/client/quantara_app/lib/features/owner_alpha/domain/realtime_market_runtime_models.dart \
            src/client/quantara_app/lib/features/owner_alpha/presentation/owner_alpha_page.dart \
            src/client/quantara_app/test/local_live_cycle_readiness_test.dart \
            src/client/quantara_app/test/local_live_runtime_resilience_source_test.dart \
            src/client/quantara_app/test/realtime_market_application_test.dart \
            src/client/quantara_app/test/realtime_production_runtime_test.dart

      - name: Restore official workflow and commit
        shell: bash
        run: |
          git fetch origin dev
          git checkout origin/dev -- .github/workflows/flutter-ci.yml
          git config user.name "Quantara Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add .github/workflows/flutter-ci.yml src/client/quantara_app
          git commit -m "fix: stabilize Local Live and realtime recovery"
          git push origin HEAD:fix/158-local-live-runtime-resilience
