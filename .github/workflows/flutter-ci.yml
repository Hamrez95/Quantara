name: Temporary repaired portfolio UI formatter

on:
  push:
    branches:
      - feat/150-portfolio-risk-budget

permissions:
  contents: write

env:
  FLUTTER_VERSION: '3.44.8'

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/150-portfolio-risk-budget
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Restore packages
        working-directory: src/client/quantara_app
        run: flutter pub get --enforce-lockfile
      - name: Format repaired UI
        working-directory: src/client/quantara_app
        run: >-
          dart format
          lib/app/quantara_app.dart
          lib/features/portfolio_risk/presentation/portfolio_risk_panel.dart
      - name: Commit formatted UI
        shell: bash
        run: |
          set -euo pipefail
          git add \
            src/client/quantara_app/lib/app/quantara_app.dart \
            src/client/quantara_app/lib/features/portfolio_risk/presentation/portfolio_risk_panel.dart
          if git diff --cached --quiet; then
            echo 'No formatting changes were required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'style(portfolio-risk): format repaired UI'
          git push origin HEAD:feat/150-portfolio-risk-budget
