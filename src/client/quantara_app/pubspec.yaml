name: quantara_app
description: Quantara explainable market research and paper-trading cockpit.
publish_to: 'none'
version: 0.10.0+8

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.6.0
  flutter_local_notifications: ^22.2.0
  shared_preferences: ^2.5.3
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
